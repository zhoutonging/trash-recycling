<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mengzhou.trashrecycling.mapper.ProductMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.mengzhou.trashrecycling.model.Product">
        <id column="id" property="id" />
        <result column="productName" property="productName" />
        <result column="productPrice" property="productPrice" />
        <result column="productDescription" property="productDescription" />
        <result column="productIcon" property="productIcon" />
        <result column="status" property="status" />
        <result column="categoryId" property="categoryId" />
        <result column="createTime" property="createTime" />
        <result column="updateTime" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, productName AS productName, productPrice AS productPrice, productDescription AS productDescription, productIcon AS productIcon, status, categoryId AS categoryId, createTime AS createTime, updateTime AS updateTime
    </sql>

</mapper>
